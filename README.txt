

Owner of the repository should create a “README.txt” file in the root folder of the repository with some text in it. 
This person should add the file to the repository, commit this change to their local copy of the repository, and then push the changes to the remote origin repository using commands similar to the following
